using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lab4 : MonoBehaviour
{

    /* Per mostare la variabile nell'inspector bisogna renderla pubblica, per fare ciò bisogna 
    aggiungere il comando "public" all'inizio della riga di codice*/

    /* Un metodo alternativo è quello di inserire la dicitura [SerializeField]
    al'inzio della riga di comando*/



    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
